# js/lib/

> 第三方库本地化目录（**P2 阶段才填充**）。

## 当前状态：暂留 CDN

为减少初期复杂度，本目录在 P1 阶段保持空置。三个第三方依赖仍从 CDN 加载：

| 库 | 版本 | 当前来源 |
|-----|------|---------|
| ECharts | 5.5.0 | CDN |
| SheetJS | 0.18.5 | CDN |
| sql.js | 1.10.3 | CDN |

## P2 计划本地化时机

| 触发条件 | 任务 |
|---------|------|
| 升级到 `@sqlite.org/sqlite-wasm` + OPFS | 必须本地化（包含 `.wasm` 资源） |
| 用户反馈离线/内网部署需求 | 全部三个本地化 |
| 引入 Vite/esbuild 等工程化工具 | 改用 npm 安装 |

## 注意事项

- 本目录下的 `.wasm`、`.bin` 等二进制资源必须在 `.gitignore` 中排除或使用 LFS。
- 升级第三方库时，必须在 [CHANGELOG.md](../../docs/修订记录/CHANGELOG.md) 中记录版本号变化。
