# v1.0.150 导入一致性、交互与权限修复

状态：2026-09-05 已完成修复、GitHub 同步、Ubuntu 部署及正式公网验收，16:57:54 发布状态确认为 `accepted`。正式入口为 https://kpi.bcyt.tech:30443 。

## 修复范围

- 完整月替换按源覆盖期间清理长险、交期及相关日级汇总，即使新结果为空也移除旧值；共享表保留未更新的转型或经代分区。
- 单独更新人力、单独更新业绩或两者覆盖月份不一致时，按旧、新业绩统计月份结合当前库内两类源重算总体和机构活动人力。
- 客户分片上传及提交互斥，文件数组按任务固定；文件变更使旧预检失效，结果不明不重复使用原确认。上传进度仅更新局部内容，成功后清理旧分析缓存。
- 职拓、客户和证保网点分析仅接收最新查询结果及错误；产品机构空选不再查询全部机构。
- 账号同角色局部更新保留自定义权限。目标数值必须有限且非负，非法输入在删除既有目标前被拒绝。
- 认证仅在单次请求内复用，保留各模块授权；同步数据库认证移入线程池。

## 导入效率和数据边界

普通同字段业绩导入的连续挂零计算由两次全库扫描降为一次，保留写入事务内完整分块校验及失败回滚。新增高优先级日期字段仍执行额外历史预检，避免漏掉跨归属月和历史缺日期问题。本次没有改变连续挂零的经营定义，也没有强行按原始年月裁剪证据。

迁移 `20260905_incremental_partition_and_hr_consistency` 标记 `requires_aggregate_rebuild=1`。已有生产库发布使用 `REBUILD_DATABASE=0 REBUILD_AGGREGATES=auto`，从当前 SQLite 原始数据重建汇总，禁止使用旧 Excel 覆盖生产历史。重建前须在一致性备份上演练并核对原始表、目标、账号和历史批次保护情况。

## 代码与回归验证

- 新增合成回归覆盖空聚合分区、双向单源更新、跨月份纠正、扫描次数、事务回滚、权限保持、目标非有限输入、请求顺序及客户上传重入。
- Windows 完整回归 `810 passed, 1 skipped, 3 warnings`；跳过项为仅适用于 POSIX 的文件锁测试。Linux CI 完整回归 `811 passed, 4 warnings`，依赖扫描无已知漏洞，镜像内容边界、构建及容器受保护接口冒烟检查通过。
- 实际 Chromium 加载当前 HTML、JS 和 ECharts，在合成接口下验证客户连续点击只创建一次上传、20 字节测试文件按 8+8+4 分片、文件输入不被进度更新替换、改选文件使预检失效、迟到结果不覆盖新筛选、产品空选传递明确空集；页面脚本错误为 0。这是浏览器交互回归，不是向生产上传真实客户文件。
- 功能提交 `1d6fbc3be186c6584fe4cb0d87ce83103fa1b35b`，PR [#27](https://github.com/lorrin328/business-analysis-template/pull/27) 合并为 `458225173884f3c10245ffe1f16c540ed8dd4e63`，合并前后代码树一致。
- GitHub PR 验证 `33953621934`、`33953694254`，主分支应用验证 `33954221061`、镜像发布 `33954221127` 均成功。GHCR `master`、`latest`、`sha-4582251` 指向摘要 `sha256:0b85bb3dd6deebf4d36d6b7c52723a6521c7965d4c5958408a53c042395c1991`。
- 正式部署源为上述合并提交的 Git 归档，SHA256 `76819408b4f8f4f9ef3ba871190cba71e2e0ed629674da6a6a610e99c0ec9104`，不包含运行数据库、Excel、日志或真实凭据。

## 生产一致性副本演练

在受保护的服务器目录对 SQLite Online Backup 一致性副本执行完整重建，耗时 887.25 秒。演练前后 2026 年严格审计均为 `status=ok, issue_count=0`，重建库 `integrity_check`、`quick_check` 均为 `ok`。

32 张原始及受保护表逐行、逐列（含原 rowid）保持一致。其中原始业绩 5,158,900 行，经代 137,519 行，人力 21,365 行，价值 684 行，目标值 6,120 行，导入记录 302 行，历史星钻批次 138 行，客户保单快照 3,100,878 行，客户主表 2,158,127 行。账号、权限、目标和历史批次均未被重建覆盖。

聚合比较排除重建产生的代理主键 `id` 和创建/更新时间，逐一核对维度与业务指标，差异为：

| 对象 | 演练前后变化 | 独立核验 |
| --- | --- | --- |
| 2024 年总体活动人力 | 12 个月×3 条业务线共 36 行由旧零值恢复，合计补回 3,193 人月 | 独立 SQL 直接按原始业绩中的月份、业务线、人员代码及有效期交条件去重，与全部 36 行一致；该合计不是年度去重人数 |
| 2024 年机构活动人力 | 188 行恢复，合计同为 3,193 人月 | 总体和机构合计一致，期初期末人力未变化，其他年份人力未变化 |
| 经代日级金额 | 9 个日期出现现有存储精度末位变化，期交合计 -4 元、规模 -2 元、折标 +1 元 | 用 Decimal 从原始经代明细独立复算；旧、新结果距精确值均不超过 0.5 元，属于万元保留四位的舍入边界；未改动任何原始金额、月度汇总或审计阈值 |
| 其余汇总 | 全部业务字段一致 | 含长险 24,250 行、连续挂零活动证据 13,957 行 |

历史 8 组保单候选键歧义和 8 行未关联事实仍按原规则保留，只作原号精确关联，不据近似编号补配。

## 正式发布与恢复验证

- 本次恢复包为 `/opt/business-analysis-backups/release-20260905_161837-255499`，冻结恢复点为 `/opt/business-analysis-backups/business_data.db.20260905_161837-255499.frozen`，大小 4,989,345,792 字节，SHA256 `fa5980175ead92bbbb585f6de33d73bc677125d54d0f4a8fc9f7006e712f2e82`，完整性及快速检查均为 `ok`。
- 2026-09-05 16:18:37 开始部署，16:21:03 停服后生成最终冻结库，约 16:24:28 开始从原始明细重建汇总；16:39:27 主服务恢复，16:39:32 部署程序正常结束。维护窗口为 18 分 24 秒；保留既有目标、复用通过自检的依赖环境，未触发回退。
- 独立 Windows 副本位于 `C:\Users\yinli\Documents\Codex\production-backups\business-analysis-v1.0.150-20260905\business_data.db.20260905_161837-255499.frozen`，目录仅授予当前用户和 SYSTEM 访问权限；与服务器冻结库 SHA256 完全一致，并已在 Windows 上实际打开 SQLite，`integrity_check`、`quick_check` 均为 `ok`。副本与元数据均不进入 Git 或同步代码目录。
- 正式公网 HTTPS 健康接口为 `v1.0.150`；主页面、客户、职拓、网点页面均为 200，匿名 KPI 为 401，数据库路径、受保护环境配置路径和 `.git/config` 均为 404。
- 本次更新的 4 份 JS 与可信归档逐字节一致。归档采用 CRLF 行尾，统一行尾后与 Git 提交内容完全一致；不将 CRLF/LF 形式差异当成版本错误，也不跳过发布包原始字节核对。
- 使用一次性、仅开放必要读取模块的测试账号完成公网登录；KPI、目标读取、职拓、客户、市场研判接口均为 200 且 `success=true`，账号管理接口为 403。实际 Chromium 在公网加载职拓页面、等待源数据展示并应用筛选，脚本错误为 0。
- 临时账号、会话、权限和登录审计已清理。验收后原有账号 15、会话 204、权限 375、操作日志 891、目标配置 2、目标值 6,120、导入记录 302、历史星钻批次 138，与冻结库逐行一致。
- 生产 2026 年严格审计为 `status=ok, issue_count=0`；主服务零重启、正常退出状态，市场研判定时器和手动监听均为 active/enabled，系统无 failed 单元，应用日志未发现错误。应用代码属 root 且运行账号不可写，运行库为 `www-data:www-data 640`。
- 最终生产对照确认 32 张受保护表逐行不变，18 张汇总表的业务字段与演练库逐行一致；客户月度事实 109,957 行及歧义键 8 行同样一致。全库 `integrity_check`、`quick_check` 均为 `ok`，无额外经营差异。
- 16:57:54 执行完整验收确认后，发布状态为 `accepted`；保留本次冻结库和完整代码、配置、依赖恢复包。机器核验记录位于服务器受保护目录 `/var/lib/business-analysis-review-v150`，独立 Windows 恢复副本继续保留。
- 验收后精确删除本次创建的 baseline/candidate 数据库及空 WAL/SHM，共释放 9,978,757,120 字节；源数据、正式恢复点、独立备份及全部核验记录保留。
- 本轮复核发现，附加对照库后不指定库名的 SQLite 完整性检查会同时扫描各附加库；本次检查已完整跑完，后续对照脚本使用 `PRAGMA main.integrity_check` / `main.quick_check` 核验目标库，避免重复检查已独立验过的对照副本。该优化仅涉及验收脚本，不更改生产数据检查标准。

## 后续优化边界

登录失败记录的有界过期清理、年度产品汇总增量语义、连续挂零完全按受影响范围重算，留作后续独立评估；本次不做无关重构。
