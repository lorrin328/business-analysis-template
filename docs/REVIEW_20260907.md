# 项目审查与产品研判调整（2026-09-07）

> 后续发布状态：用户授权提交GitHub并部署后，v1.0.152已通过PR #29上线Ubuntu，方案复核线上入口已移除。完整验收与恢复点见[发布记录](RELEASE_REVIEW_v1.0.152.md)。下文“仅本地删除/仅市场组件发布”为前一阶段记录。

## 核心结论

审查覆盖UI交互、计算逻辑和展示字段，形成11项具体问题：本次市场页修复3项，其余8项保留为有证据的整改清单。方案复核在本地源码中完整删除，历史数据库记录保留。寿险产品专题已完成来源搜索、生成合同、字段展示、证据校验和缺口处理；Ubuntu采用市场研判组件定向更新，不包含本地方案复核删除。

## 优先整改

| 顺序 | 问题 | 影响 | 建议动作与节奏 |
|---|---|---|---|
| P1 | 产品件数缺失时被保费替代 | 金额冒充件数 | 下一次修复先移除金额兜底；前端负责人补缺失件数回归 |
| P1 | 人力机构全空选仍展示全平台 | 筛选和结果相反 | 下一次修复统一空选语义；前后端共同验证空/单/全选 |
| P1 | 客户新客经营更改筛选后返回概览仍用旧缓存 | 旧数据挂在新筛选下 | 下一次修复按查询键失效缓存；验证跨标签切换 |
| P2 | 产品件数图单位为“万” | 单位误读 | 与件数兜底修复同批处理；披露经代源行数限制 |
| P2 | 产品超过20类静默截断 | 结构占比分母缩小 | 返回全量或归并其他，验证全量勾稽 |
| P2 | 经代10年交日来源可用性误用其他业务记录 | 月表有数却返回日级0 | 按来源和范围检测，恢复月级回退与精度提示 |
| P2 | 客户/网点/职拓失败后仍留旧KPI | 新筛选对应旧数 | 标注上次成功范围，或清空失效结果 |
| P2 | 网点147/86写死 | 标题与参考数据可能不一致 | 由接口元数据生成标题，并披露固定全量口径 |

上述为代码及合成数据验证，不声称全部已在真实生产数据中发生。当前任务对这些模块做review，不擅自改变经营统计口径。计算详情见 [计算与字段报告](REVIEW_CALCULATIONS_20260907.md)，交互详情见 [UI与字段报告](REVIEW_UI_20260907.md)。二者附模块字段、公式、单位、截止和不可计算边界。

## 已完成变更

- 方案复核：移除导航、HTML/JS、API及专属计算包、权限配置、nginx旧入口与镜像校验要求。历史表、权限行和发布记录保留，旧权限不再生效；本地旧入口回归为404。
- 市场产品：新增“寿险产品”筛选，产品仍纳入同业层；展示名称、公司、责任类型、利益机制、适用渠道、销售状态、缴费期间、保险期间、保证与非保证利益10项字段。
- 产品证据：搜索至少3类产品主题；产品事实必须引用一手证据。名称和公司可在原文定位，其他参数保留完整原文片段，防止“不保证”被截成“保证”或负号丢失。缺失字段明确标注；无证据时披露缺口而非凑产品。
- 市场交互：历史晚到请求不覆盖当前选择，刷新保留历史选择，无观察样本显示“待积累”；手机顶部按钮改为分行排列。

## 验证边界

- 本地完整回归：837 passed、1 skipped（Windows不支持POSIX锁），1条既有依赖弃用警告。最终产品证据、市场及删除入口专项：65 passed（含历史异步测试调用）；历史异步行为6项通过。
- 方案复核移除专项：92项通过，包含管理员访问旧页面、脚本、查询、上传404和历史权限不再生效。
- 产品界面使用明确标注的合成数据实际渲染，1440px及390px无横向溢出、无脚本错误，10个字段及8个缺失标签正确。不是真实产品研判或生产数据复算。
- 未进行全库逐项生产金额对账；未把合成样例作为正式研判发布；没有推送GitHub或全量部署主应用。

## Ubuntu市场组件发布验收

- 市场组件基于已提交代码 `9e4721c8bb333bb23b6702daf3f476304f04677f` 生成可信定向归档，归档SHA256为 `e652d9ed0705258eb72de9f9e0e21ed43ce0e43dcac5cb8cc530d09e13cbf5b0`。发布前逐文件确认仅存在换行差异，采用服务器实际SHA256作为冻结基线；发布后与归档逐字节一致。
- 恢复点：`/opt/business-analysis-backups/market-products-20260907-1788761423`，包含原组件与受保护配置，目录仅root可访问；`release.json`状态为`accepted_market_component`。
- 生产 `MARKET_ANALYSIS_REQUIRE_PRODUCT_RESEARCH=1`；来源侦察、主研和修复继续使用`deepseek-v4-flash-vision-exp`。真实CC最小结构化调用通过；只读dry-run读取6期历史、46个主题，确认产品合同与保证/非保证字段进入提示词。
- FastAPI健康、正式公网HTTPS健康通过；页面与JS内容哈希一致。市场API未登录401；临时授权会话读取latest/history/status均200且success=true，验证后会话已删除。
- 主应用、市场timer和手动监听均active。未重启主应用、未重建或导入经营数据、未切换模型。latest.json和status.json在验证前后哈希不变，最新有效报告仍为`market-20260904-150238`。
- 没有运行整轮研究或发布合成产品报告；下次定时/手动研究开始应用产品规则，生成结果仍需通过独立来源核验和9分门槛。
- 发布范围仅8个市场相关文件及产品开关，未发布本地方案复核删除；主应用仍为v1.0.151。源码删除需要后续主应用正式发布后才会移除线上入口。
