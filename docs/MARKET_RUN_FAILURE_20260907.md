# 市场研究运行失败修复（2026-09-07）

- 生产手动启动已于15:54:49成功。16:27:36触发模型阶段预算上限后，systemd自动重试；第二次于16:51:55再次预算失败。
- 检查点剩余错误为M9的利益机制、渠道、销售状态简写，与独立核验后替换的完整来源摘录不一致。
- 修复：核验后的可选字段若失去直接支持，则不展示该字段并记入productResearch.gaps；产品名称和保险公司仍为必填校验。检查点恢复应用同一处理。
- 修复：发布前后续修订按已配置升级档位读取预算/轮数；预算耗尽返回不自动重试的退出码2并显示中文原因。未修改服务器预算配置和模型。
- 验证：65项市场研究测试通过（含必填身份不可绕过、可选字段证据变化、预算退出码回归）。真实模型调用与新报告发布尚未完成验证。

## 综合门禁补充复核

16:59:20运行从检查点恢复，产品字段校验通过；16:59:24因质量分8.60低于9.00终止，未调用模型。原因包括恢复过程丢失主调用记录，以及12个延续主题仅9个有新增证据。按运行日志中的实际整条调用链作只读诊断，分数为8.80（不是9.20，历史升级修订仍影响可靠性得分）。缺少新增证据：M2制造业PMI、M6网络营销规则、M8新华中报。

修复：检查点单独保存worker实测modelCalls，恢复时用于报告质量评估；本次运行统计仍只记本次调用，避免成本重复累计。模型生成的report字段不作为可信调用证明。开启质量门禁时，缺少实测主调用证明的旧检查点不再进入修订循环，改走完整新研究；旧文件保留到新检查点正常写入。发布阈值、模型和环境预算均未调整。

验证：67项相关测试通过，包括真实调用记录恢复、模型伪造元数据不得作为主调用证明、恢复不重复记账。当前生产草稿完整评分仍未达到9分，未发布新报告；尚未验证下一次完整研究成功。

## 17:14运行及局部修订改造

本轮17:14:24启动，17:42:03以退出码2结束。来源侦察、主研究调用已完成；首次repair_flash触发3 USD CLI估算预算上限。检查点有15项错误，涉及8个模块分析中的未支持数字/日期/政策词，以及2个产品模块的身份或其他字段证据不匹配。预算失败不自动重试已经生效。不能把CLI估算等同DeepSeek实际账单。

原修订接口要求再次输出整篇报告，改为结构化局部patch：仅返回受影响字段；程序保留其余内容，白名单限制可修改字段，禁止模型写入质量评分、调用记录和来源核验结果。来源修改后清除旧核验状态并重新取证。完整结构、证据、跨期和质量校验继续执行；发布门槛、模型、预算不变。完整报告返回保留旧CLI兼容路径，但新调用显式使用patch输出schema。

验证：74项市场相关测试通过，包括局部patch完整worker发布流程（模拟模型及外部取证）、原稿不可变、未改字段保留、保护字段拒绝、来源重新核验。尚无真实模型预算节省或新报告成功发布证据。暂不应以连续重跑替代验收。
