# 待确认问题

## 2026-07-22 市场研判上线前阻塞

- 用户在聊天中提供的 DeepSeek API Key 已暴露，必须先在服务商侧吊销并生成新 Key；新 Key 只能通过服务器受保护环境文件或密码管理器注入，不能再次粘贴到聊天、命令、项目或日志。
- 生产 `.ai_env` 中的 `AI_READONLY_TOKEN` 历史上也存在可能暴露风险；市场研判服务启用前应一并轮换，并将新值安全写入 `/etc/business-analysis-market/market-analysis.env`。
- 生产当前仍显式开启公开自助注册。市场研判普通用户默认无权限，但仍建议关闭公开注册并采用管理员审批开通，避免内部经营判断页面的账号边界失控。

## 2026-07-14 审计待确认

- 【静态暴露和 root 链已修复 2026-07-20】nginx 历史日志确认敏感路径曾有 `11` 次 200、来自 `2` 个私网客户端；未发现公网客户端但无法证明文件正文未被读取。仍需轮换 `.ai_env` 等现用凭据并撤销旧 Session；生产服务是否始终仅限可信内网仍待确认。
- 生产公开注册当前为开启状态：是否改为管理员审批开通；普通用户默认可读经营、机构、队伍、荣誉和方案结果是否符合数据保密边界。
- KPI 混合口径最终采用“转型/经代各自真实截止日”还是“共同最早截止日”；当前代码与指标说明文档不一致。
- 平台 KPI 是否应包含十家机构范围外的“深圳/其他”；如包含，机构表需明确不可与平台总量直接求和勾稽。
- 【页面定位已落实 2026-07-20】v1.0.103 已统一展示为“方案底稿复核”，并明确非最终发放结果；在推荐人、有效保单和独立规则计算未完成前，仍不得作为自动结算模块。

## 2026-07-09 组织发展方案口径待确认

- “方案统计人力”起算日是否统一按正式通知方案期 `2026-07-01`，并同步修正底稿 `参数表!B2`。
- 推荐人奖励的可计算字段来源：需要确认推荐人代码、推荐关系、推荐人当月是否活动、同一推荐人多团队奖励上限和展示口径。
- 有效保单条件的数据来源：45 日内撤保退保、回执回访、犹豫期、自保互保、保单有效性是否由保单明细新增字段提供，还是由人工复核后导入。
- 主管/经理“当月活动人力”校验是否必须作为入职主管、入职经理和推荐人奖励的前置条件；当前 Excel 仅部分体现。
- 组织育成奖发放对象需确认最终展示：正式通知为晋升人员原主管或无主管时原经理，当前底稿结果挂在晋升主管行。
- “维持方案资格”的 70% 判断当前按团队首期标保标准执行，是否完全不适用于架构人力、主管架构和开单率条件，需在最终结算前确认。

## 2026-07-01 荣誉体系口径待确认

- 【已解决 2026-07-02】同事完整清单为 V:AB 区域 `59` 个会员身份轨道；系统原重算 `61` 的差异来自冲销/退保净额和显式 0 件处理。修正后本地真实库 2026 年 6 月重算为 `59`，逐人差异为 `0`。
- 【已解决 2026-07-02】证保季度通算边界：用户已确认同事底稿正确，系统改为按自然季度固定 3 个月判断，每个月均需有长险且季度标保合计不低于 `9万`；不再按季度在职月数缩短判断周期。
- 【已按方案修正 2026-07-01】长险件口径：星钻规则中的“长险 >= 1 件”按 `performance.长短险` 和缴费年限严格识别，短险不计入。
- 【已按方案修正 2026-07-01】45 天回销口径：未设置 `HONOR_AS_OF_DATE` 时按统计月月末后 45 天判断；如后续需要按导入截止日或批次固定日期，应新增批次字段。
- 【已按方案修正 2026-07-01】历史重算人力状态：重算历史月份时按所选月份或已有最近月份月末在职状态，不再用未来月份清零历史批次。
- 【已按方案修正 2026-07-01】管理职团队星钻：主管/经理个人达标和团队达标可同月分别获钻；页面已将团队历史 `star_manpower_count` 标注为“团队会员人数”。
- 管理职团队归属：当前仍依赖业绩表或人力表中的主管/经理工号、营业组/营业部字段；若正式 Excel 存在更权威的团队归属字段，需确认并优先使用。
- 【已解决 2026-07-02】荣誉看板等级分布：dashboard `levels` 定义为已入会会员等级分布，只统计会员身份轨道；全量追踪池另由追踪人力、累计追踪池和人员明细承载。
- 品质与资格规则：自保互保、投诉、违规、继续率、合同制外勤等方案要求尚无完整源字段，需确认是否接入新清单或暂作为人工复核项。

## 2026-06-20 审计待确认

- 生产访问范围：当前生产已按用户要求显式开启公开自助注册；系统是否仅限内网可信用户访问、是否需要 HTTPS/外网访问策略和账号审批流程仍需确认。
- 自动部署外网可达性：服务器端 webhook 已修复并完成签名 ping，但 GitHub webhook URL 为 `http://192.168.50.6/webhook/deploy` 内网地址；若 github.com 云端无法访问，需要公网可达地址、VPN/隧道或自托管中转。
- 普通用户权限：普通用户默认可读 KPI、机构、平台趋势、产品结构、交期结构、队伍和荣誉查看，是否符合经营数据保密要求。
- 本地运行库：是否需要立即用根目录 2026-06-19 Excel 重建本地 `backend/business_data.db` 并恢复正式目标，使本地环境与服务器一致。
- 交期结构：业务是否要求日级 `asOf` 精准切换；若要求，需要调整当前月级 `agg_payment_period` 设计。

## 2026-06-19 本地库与服务器库差异

- 服务器端已确认 2026 目标配置正常、经代期交年度目标为 `81900` 万，且 4 份 `20260619` Excel 已成功导入。
- 当前待确认仅限本地开发库：是否需要用根目录最新 Excel 重建 `backend/business_data.db`，使本地页面与服务器数据完全一致。

## 2026-06-19 交期结构日级口径

- 当前交期结构来源 `agg_payment_period` 只有月级字段，`asOf` 只能截到月份。
- 待确认：业务是否要求交期结构在 6 月 18 日/6 月 19 日之间也能精确切换；如需要，应新增日级交期聚合或改为原始明细实时聚合。

## 2026-06-13 容器部署

- GHCR 镜像包是否需要公开访问，还是保持私有并由服务器使用 GitHub token 登录拉取。
- 生产环境是否继续保留 nginx 反向代理，或改为直接由 Docker Compose 暴露 `45679` 后再接入宿主机 nginx。
- 容器部署时管理员 Token、AI 只读 Token 等环境变量的正式命名和生成规则需按现有鉴权实现最终确认。

## 2026-06-13 Python 版本文件

- 当前 `.python-version` 为 `3.10`，项目实际已在 Python 3.12.10 上通过完整测试。
- 待确认：是否保持 `3.10` 作为最低兼容基线，还是调整为团队推荐版本。
