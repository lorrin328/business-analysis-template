# 待确认问题

## 2026-06-20 审计待确认

- 生产访问范围：公开自助注册已在生产环境默认关闭，但系统是否仅限内网可信用户访问、是否需要 HTTPS/外网访问策略和账号审批流程仍需确认。
- 普通用户权限：普通用户默认可读 KPI、机构、平台趋势、产品结构、交期结构、队伍和荣誉查看，是否符合经营数据保密要求。
- 本地运行库：是否需要立即用根目录 2026-06-19 Excel 重建本地 `backend/business_data.db` 并恢复正式目标，使本地环境与服务器一致。
- 交期结构：业务是否要求日级 `asOf` 精准切换；若要求，需要调整当前月级 `agg_payment_period` 设计。

## 2026-06-19 本地库与服务器库差异

- 服务器端已确认 2026 目标配置正常、经代期交年度目标为 `81900` 万，且 4 份 `20260619` Excel 已成功导入。
- 当前待确认仅限本地开发库：是否需要用根目录最新 Excel 重建 `backend/business_data.db`，使本地页面与服务器数据完全一致。

## 2026-06-19 交期结构日级口径

- 当前交期结构来源 `agg_payment_period` 只有月级字段，`asOf` 只能截到月份。
- 待确认：业务是否要求交期结构在 6 月 18 日/6 月 19 日之间也能精确切换；如需要，应新增日级交期聚合或改为原始明细实时聚合。

## 2026-06-13 容器部署

- GHCR 镜像包是否需要公开访问，还是保持私有并由服务器使用 GitHub token 登录拉取。
- 生产环境是否继续保留 nginx 反向代理，或改为直接由 Docker Compose 暴露 `45679` 后再接入宿主机 nginx。
- 容器部署时管理员 Token、AI 只读 Token 等环境变量的正式命名和生成规则需按现有鉴权实现最终确认。

## 2026-06-13 Python 版本文件

- 当前 `.python-version` 为 `3.10`，项目实际已在 Python 3.12.10 上通过完整测试。
- 待确认：是否保持 `3.10` 作为最低兼容基线，还是调整为团队推荐版本。
