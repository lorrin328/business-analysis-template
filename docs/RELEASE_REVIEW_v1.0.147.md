# v1.0.147 上传修复与发布复核

状态：2026-09-03 已上线，四份生产清单已成功导入，数据与运行复核通过。本轮未发现需要回退的新增问题。

应用提交为 `2c78e3bec71b7b17ed8bbdffcebf3acb40eb5c57`，对应 [PR #25](https://github.com/lorrin328/business-analysis-template/pull/25)。正式入口为 https://kpi.bcyt.tech:30443 。后续文档提交不改变生产应用归档。

## 问题与修改

- 用户选择四份日常清单后，预览长期停留在解析状态。旧版以月份和字段重复扫描数百万行历史数据；前端没有覆盖响应正文读取的超时与取消机制。实际旧页面最终返回经代文件存在无法识别统计年月的错误。
- 经代表末尾的明确“合计”行被当作明细拒绝。本版仅忽略末尾、非金额维度为空的明确总计行，并在预览说明；中间异常行、带明细维度的无效期间仍拒绝，不放宽真实明细校验。
- 原始期间筛选改为一次日期规范化和批量期间判断；关键字段保护改为单次聚合检查，仍保护有效零及旧字段，按原有事务回滚规则处理写入失败。
- 预览支持取消和120秒超时，释放按钮且保留文件；旧响应不能覆盖新请求状态。写入请求300秒超时或结果不确定时提示先核对，阻止自动重试；刷新后仍保留待核实保护，不保存文件内容或客户信息。
- 仅修改上传解析、预检效率和请求状态管理，未调整业务聚合公式、目标或客户匹配规则，未新增需要历史重建的迁移。

## 回归与独立演练

- 最终 Linux 全量回归731项通过；Windows730项通过、1项POSIX检查由Linux验证。依赖扫描无已知漏洞，实际镜像内容与启动检查通过。
- 新增70项期间与性能回归、7项经代末尾合计回归，前端累计13种状态流场景。覆盖跨格式月份、缺失字段、真实零、覆盖边界、事务回滚、挂起响应正文、取消、迟到响应和重复提交防护。
- 在生产在线一致性备份上隔离演练同一组四份文件：预览47.25秒、导入172.71秒，四份全部成功，错误0。全库完整性ok，目标/账号/权限及未覆盖的旧年度主汇总保持一致。
- PR验证 `33705874026`、合并后应用验证 `33706019111` 和镜像发布 `33706019308` 均成功。可信归档SHA256：`3545b5f22ec883d213277d464686d21576576f56ac16f3e516713d7bfd94a162`。

## 生产发布与导入

- 使用已提交归档部署，`REBUILD_DATABASE=0`，复用通过检查的既有依赖环境，跳过无新增迁移的聚合重建。主服务于北京时间2026-09-03 10:09:59启动，版本v1.0.147。
- 生产重新预览46.08秒通过。所选文件与隔离演练的内容指纹一致；导入锁内确认生产最新导入记录仍为290后才写入。
- 正式导入181.88秒，2026-09-03北京时间10:15:48提交，新增导入记录291—294，四份均为success，错误0、跳过0。业绩源5,158,768→5,158,830行，经代137,386→137,456行，人力保持21,362行，价值222→226行；客户主表2,158,127行和保单快照3,100,878行保持不变。
- 自动浏览器选文件接口返回 `Not allowed`，没有放宽浏览器权限或应用鉴权。本次改由管理员在服务器调用与Web共用的预览和导入服务，实际写入以www-data身份执行、使用生产导入锁及完整事务。网页文件选择至提交的完整链路未完成自动验收，不能以服务器成功替代这一项。
- 已验证正式公网16MiB无鉴权请求返回401，没有413；公网新上传脚本与可信归档逐字节一致。缓存标识使用规范化脚本内容摘要，实际传输字节SHA256为 `c08c260a64c09c0fa3464c6ddd3ac0810d6c2910104fb8eac5f3f04959e34d5f`。
- 已登录浏览器刷新后展示新数据，统计范围累计至2026-09-02，机构连续挂零截止日从9月1日更新至9月2日；未选文件时确认导入按钮禁用。页面错误日志为空，服务active/running、NRestarts=0、ExecMainStatus=0，启动后未发现500或异常堆栈。
- 最终生产完整性与快速检查均ok；2026年只读审计零问题，2025年仅保留与候选及导入前一致的既有差异。18张汇总表所有业务字段与候选一致；初次全行比较的差异逐列证实仅为不同运行时刻产生的created_at，未忽略其他字段。客户事实业务字段和输入文件指纹一致。
- 账号、权限、目标配置与目标值相对停服冻结库逐行相同；未覆盖旧年度的主汇总相同。正式公网HTML和上传脚本与归档一致，匿名业务接口401、敏感路径404，合法AI只读请求200并返回8张指标合同。

## 已知边界

- 2026年候选及最终生产只读审计均零问题。2025年有一项既有机构日汇总差异：规模保费汇总比从原始明细重算高0.01万元。导入前、候选及最终生产差异完全一致，未因导入扩大；本轮不修改无关历史汇总，也不放宽审计阈值。
- 历史8组保单编号歧义仍按原号精确关联，未知归属不作推定。继续率来源、经代价值来源、历史凭据轮换等边界沿用v1.0.146记录。

## 恢复点与私有证据

- 停服冻结库：`/opt/business-analysis-backups/business_data.db.20260903_100216-230794.frozen`，配套 `.meta`；代码、配置与恢复工具位于 `/opt/business-analysis-backups/release-20260903_100216-230794`。
- 冻结备份4,989,345,792字节，SHA256为 `ccadd79bd8ab33b3a4980111248e8a1730e41c17ef4d6348cd1c27149602d473`，完整性与快速检查均ok。
- 完成上述复核后执行受保护的accept-release，状态accepted；保留本次冻结库与完整恢复包，未触发代码或数据回退。上一版独立Windows备份仍在代码同步目录外保留。
- 私有预览、导入及复核结果位于 `/var/lib/business-analysis-reviews/20260903-upload`。候选数据库、源文件、客户明细和运行凭据均不提交Git或进入镜像。
- 上线后已授权导入新数据，后续若需回退必须先核对并保护新增写入，不能直接用发布前冻结库覆盖当前生产库。
