# v1.0.45 发布说明

## 发布日期

2026-05-24

## 发布类型

fix / deploy

## 修复内容

- 移除 `deploy/deploy.sh` 中 `webhook.py` 复制到自身的无效命令。
- 避免部署脚本在 `set -e` 模式下因自拷贝返回错误而中断。

## 验证重点

- Ubuntu 部署脚本可继续执行到 systemd 与 nginx 重启阶段。
- 页面版本号统一为 `v1.0.45`。
