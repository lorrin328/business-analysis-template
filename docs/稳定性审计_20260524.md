# 稳定性审计记录（2026-05-24）

## 核心结论

1. 当前代码测试基线稳定：归档前后均验证 `108 passed, 8 skipped`。
2. 部署路线已收敛为 `Nginx + systemd + FastAPI + SQLite`，Docker 相关文件不再作为生产链路维护。
3. 已将不参与运行链路的 Docker 文件、本地运行产物、旧构建目录、历史备份和临时输出归档至 `bak/20260524_stability_archive/`；Excel 文件保留。
4. 2026 年数据质量审计结果为 `status: ok`，发现问题数 `0`。

## 问题诊断

| 类型 | 现状 | 稳定性影响 | 处理 |
|---|---|---|---|
| Docker 文件 | 与当前部署路线不一致 | 后续维护时可能误用旧部署方式 | 归档到 `deprecated_docker/` |
| Docker CI | `.github/workflows/docker.yml` 仍会构建镜像 | 推送后产生无效构建与维护噪音 | 归档到 `deprecated_docker/` |
| 本地依赖目录 | `node_modules`、`backend/venv` 存在 | 体积大、不可迁移、容易污染同步 | 归档到 `local_runtime_artifacts/` |
| 运行缓存 | `__pycache__`、`.pytest_cache` 存在 | 无业务价值，影响审计清晰度 | 归档到 `local_runtime_artifacts/` |
| 历史备份与输出 | 旧 DB 备份、`dist/`、`output/`、临时文本存在 | 容易与当前生产入口混淆 | 归档到 `old_backups_and_outputs/` |

## 归档清单

### Docker 弃用资产

- `.dockerignore`
- `Dockerfile`
- `docker-compose.yml`
- `docker-compose.nginx.yml`
- `docker-entrypoint.sh`
- `deploy/nginx-docker.conf`
- `docs/Docker部署说明.md`
- `.github/workflows/docker.yml`

### 本地运行产物

- `node_modules/`
- `backend/venv/`
- `.pytest_cache/`
- `backend/.pytest_cache/`
- `backend/__pycache__/`
- `tests/__pycache__/`

### 旧备份与临时输出

- `.git.corrupt-backup/`
- `dist/`
- `output/`
- `_cleanup_backup_20260513_092849/`
- 根目录旧 `business_data.db`
- `backend/business_data.db.bak.*`
- `dtmpgetModalContent.txt`
- `excel/output1.txt`
- `excel/output2.txt`

## 保留边界

- 保留 `backend/business_data.db`：当前 FastAPI 默认运行库。
- 保留根目录业务 Excel：部署脚本可用于自动重建数据库。
- 保留 `excel/` 下 Excel 模板：按用户要求 Excel 不归档。
- 保留 `deploy/deploy.sh`、`deploy/nginx.conf`、`deploy/systemd.service`、`deploy/webhook.py`、`deploy/webhook.service`：当前生产部署链路仍依赖。

## 后续建议

1. 每次上线前执行：`uv run python -m pytest -q`。
2. 服务器部署前确认：`ADMIN_TOKEN` 已写入 `/opt/business-analysis/deploy/.admin_env`。
3. 上传 Excel 后执行数据质量审计：`cd backend && python audit_data_quality.py --year 2026`。
4. GitHub 如仍有旧 Docker Actions 运行，需要确认本次归档变更已提交并推送。
