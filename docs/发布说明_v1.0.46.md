# v1.0.46 发布说明

## 发布日期

2026-05-24

## 发布类型

fix / deploy

## 修复内容

- `deploy/deploy.sh` 的 rsync 同步排除 `backend/venv`。
- 部署时删除并重建生产虚拟环境，避免 `uvicorn` 脚本 shebang 指向源码仓库路径。

## 验证重点

- `business-analysis.service` 能通过 `/opt/business-analysis/backend/venv/bin/uvicorn` 正常启动。
- 页面版本号统一为 `v1.0.46`。
