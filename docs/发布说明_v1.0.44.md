# v1.0.44 发布说明

## 发布日期

2026-05-24

## 发布类型

stability / refactor / deploy

## 核心变更

1. 稳定性增强：新增真实健康检查、写库串行锁、部署前预检脚本。
2. 部署收敛：去除 Docker 路径，保留 Ubuntu systemd + nginx 部署方式。
3. 前端精简：生产页面拆分为多个明确运行模块，主 HTML 回归页面壳与启动入口。
4. 扩展性增强：指标配置接口新增 `dashboardKpiCards`，前端启动时读取统一配置。

## 验证结果

- 本地预检：`preflight ok`
- 自动测试：`116 passed, 8 skipped`
- 数据质量审计：`status=ok`，`issues=0`

## 运维影响

- 部署方式继续使用 `sudo bash deploy/deploy.sh`。
- Docker 文件已归档，不再作为推荐部署路径。
- Excel 文件未归档到 `bak`，生产数据文件仍按原路径保留。
