# 本轮验证记录

日期：2026-09-16；源码：2084e52；版本标记：v1.0.155。

## 既有回归

- 完整 pytest：901 passed、3 skipped、1 warning，60.33s。
- 命令：`python -m pytest -q --basetemp <临时目录>/business-review-20260916-pytest`。
- 已设置 `PYTHONUTF8=1`、`BUSINESS_ANALYSIS_DB=<临时目录>/business-review-20260916-test.db`，隔离默认业务库。
- 跳过范围：Windows不适用的POSIX锁、Linux Shell，以及当前不可用的实际Docker构建验证。
- 警告：Starlette TestClient使用httpx的弃用提醒。
- 独立Node：`node --test tests/*.test.cjs`，53 passed、0 failed。部分测试已包含在pytest包装中，不合计为954个独立测试。

## 独立反例

三组脚本由审查工作流生成，主审再次实际执行，退出码均0；详见同目录txt。脚本成功表示复现了旧行为，不表示缺陷已经修复。

- `metrics_repro.py` / `metrics_repro.txt`：内存库、合成Excel和DataFrame；包括完整月级导入链。
- `ui_repro.cjs` / `ui_repro.txt`：读取当前真实源码，VM执行机构渲染、产品/交期请求；输出合成结果。
- `platform_repro.py` / `platform_repro.txt`：内存星钻行数、Excel类型、真实诊断表达式和测试旁路环境判断。

## 浏览器检查

本地临时服务，仅监听127.0.0.1；临时独立库中只复制本地部分汇总/目标表，未复制真实用户、客户、保单或人员明细。登录使用临时本机测试账号，不使用生产凭据。

主看板使用1280像素宽度和390×844手机尺寸检查；其他页面以默认桌面宽度抽查。覆盖主看板、客户、网点、职拓、荣誉、人员测算、税优、市场的基本入口与状态。客户/网点/荣誉/市场没有复制专题完整数据，因此只评价相应空/缺失状态，不推断生产数据缺失。

实际浏览器确认人员测算产品系数输入0后，1万元人产仍显示个人NFYP10000。接口返回客户/网点缺失源的具体说明，但页面仅显示API路径和422。荣誉尚无批次时总人数显示—而会员率0.0%。市场页显示每3天，与本地调度代码5天不符。

没有完成全部筛选组合、长时间运行、生产并发或移动设备触屏验证；没有对最新税收政策和基本法制度有效性做外部核验。

## 只读与交付边界

应用源码未更改，原业务库只读；新增文件仅为本次报告及合成验证底稿。未访问生产服务器、未部署、未运行真实市场研究、未提交Git。
