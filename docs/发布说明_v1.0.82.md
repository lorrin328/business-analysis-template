# v1.0.82 发布说明

## 发布类型

修复版本：标准人力展示位置调整与现网部署状态核验。

## 核心变更

1. 将“标准人力贡献分析”放到“队伍结构与产能分析”模块下方，位于司龄结构、产能段、分位数和机构分位数表之后。
2. 保留 v1.0.81 标准人力计算口径与后端数据结构，不调整计算公式。
3. 统一页面缓存版本到 v1.0.82，避免浏览器继续使用旧脚本。

## 现网排查结论

- Ubuntu 服务器代码已是 v1.0.81，但服务进程未重启，导致 HTTP 接口仍使用旧内存代码，未返回 `standardManpower` 字段。
- 重启 `business-analysis` 服务后，接口已正常返回标准人力数据。
- 2026年5月现网接口抽查：整体标准人力 75 人，标准人力期交贡献 1,893.71 万。

## 验证

- 全量测试通过。
- `git diff --check` 通过。
- 现网 `/api/health` 与 `/api/team-enhanced-analysis` 已完成抽查。

