# v1.0.148 证保网点太平人员展示与发布复核

状态：2026-09-04 已上线并完成正式公网验收，发布状态为 `accepted`。本轮仅增加证保常规网点的太平人员展示及对应说明、测试，不改变数据库结构、业务聚合公式或生产数据。

功能提交为 `5cf2a65b84ddc23a9c04eff03ca8baf2c2149c70`；Linux换行兼容修复后，最终应用提交为 `5490a546cee29e2bbd2e9977b8281816be5a6b60`。正式入口为 https://kpi.bcyt.tech:30443 。后续发布记录提交不改变生产应用归档。

## 功能与口径

- “证保网点分析—常规网点”表格在“券商人员”后新增“太平人员”，展示该网点当期有承保业绩的太平人员数量。
- “券商人员”和“太平人员”均按对应人员代码去重，避免同一人员多件保单造成重复计数；无值按0展示。
- 后端原接口已提供 `internalAgents` 字段，本次前端直接展示该字段；未新增数据库迁移，未调整网点、保费、件数、活动月等既有口径。
- 页面说明和数据字典已同步补充两类人员的定义。

## 回归与持续集成

- Windows全量回归730项通过、1项跳过，2026年本地数据审计状态为ok、问题0；36项Node前端测试及脚本语法检查通过。
- 初次Linux验证发现静态脚本摘要受CRLF/LF换行影响，已改为先规范化换行再计算摘要；该问题只影响跨平台校验，不改变业务功能。
- 最终GitHub应用验证运行 `33839312086` 成功，Docker镜像运行 `33839312237` 成功；构建、容器启动和安全检查全部通过。
- 可信应用归档共463个条目，不包含数据库、Excel、日志或秘密文件；归档SHA256为 `7f39c44694a84e0d1e8357a1b1685c8430f91ad000311308f84c4e07a47628b5`。

## 生产发布与数据保护

- 从最终提交生成可信归档并部署，使用 `REBUILD_DATABASE=0 REBUILD_AGGREGATES=auto`；复用既有依赖环境，无新增强制重建迁移，未重建生产数据库或历史聚合。
- 发布前后核心表行数完全一致：`performance` 5,158,875行、`jingdai` 137,519行、`hr_data` 21,365行、`value_data` 226行、`branch_reference` 233行；业绩截止为2026-09-04。
- 发布后SQLite `quick_check`、`integrity_check` 均为ok，55张表完整；2026年生产严格数据审计状态为ok、问题0。
- 主服务active/running、enabled，`NRestarts=0`、`ExecMainStatus=0`，北京时间2026-09-04 13:21:12进入运行状态；发布后日志未发现warning至alert级别新增记录。

## 正式公网与浏览器验收

- FastAPI本机与正式NPM公网健康接口均为v1.0.148，页面版本与应用版本一致，最新期间为202609，缺失表为0。
- 根页面和网点分析页面均返回200；匿名访问网点分析业务接口返回401，`/.env`、`/.git/config`、`/backend/main.py`、`/business_data.db` 均返回404。
- 服务器安装的HTML和JS与可信归档逐字节一致；归档、服务器和正式公网JS的SHA256均为 `ec4c5ed902e11375500ee9a53fe8bbfa51ef839cf3b6ee2e88932bdb3cbd3632`，页面引用缓存标识为 `v1.0.148-691fb77a4058`。
- 正式公网登录后进入“网点分析”，页面显示数据截止2026-09-04、147/147个常规网点；表头按“券商人员—太平人员—活动月”排列。首行实际显示券商人员16、太平人员13、活动月7，浏览器控制台无错误或警告。
- 验收临时账号及其权限、会话和操作日志已清理并复核为0，不保留测试访问主体。

## 恢复点与已知边界

- 在线备份：`/opt/business-analysis-backups/business_data.db.20260904_131243-238737`。
- 停服冻结库：`/opt/business-analysis-backups/business_data.db.20260904_131243-238737.frozen`，大小4,989,345,792字节，SHA256为 `c0f420592af1d838a5d401fbe2eb630c0a9e8f7f28e74958dd94db9ac5097433`，完整性与快速检查均ok，表数55。
- 完整恢复目录：`/opt/business-analysis-backups/release-20260904_131243-238737`。完成代码、数据、鉴权、静态资源和浏览器复核后已执行受保护的accept-release，状态accepted；未触发回退。
- 同机另有与本项目本次改动无关的 `market-analysis.service` 处于failed状态；本轮未获得修复该独立服务的授权，未对其修改。该状态不影响已核验的经营分析主服务和本次证保网点页面，但应另行排查。
